# Workflow export

Place a manually reviewed export here as `workflow.sanitised.json`.

Remove the assistant prompt, credential references, WhatsApp instance details, local model host, account names, calendar IDs and task-list IDs. Synthetic placeholders are preferable to an export that appears runnable but still contains internal references.
