# Workflow export

Place a manually reviewed export here as `workflow.sanitised.json`.

The export should be safe to inspect, but it does not need to be directly runnable. Replace credentials, webhook paths, document IDs, live URLs and client-specific constants with clear placeholders.
