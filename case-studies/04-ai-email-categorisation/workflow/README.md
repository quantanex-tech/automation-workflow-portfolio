# Workflow export

Place a manually reviewed export here as `workflow.sanitised.json`.

Remove mailbox IDs, folder IDs, credential references, Nextcloud paths, real categories that reveal client processes and all pinned message data. Keep a simplified fixed taxonomy if the original category wording is commercially sensitive.
