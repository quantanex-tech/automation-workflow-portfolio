# Workflow export

Place a manually reviewed export here as `workflow.sanitised.json`.

Replace Vapi assistant and phone-number IDs, webhook paths, document IDs, calendar IDs and all test contact data. Remove any pinned call payload or transcript.
